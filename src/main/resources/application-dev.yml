server:
    port: 8080
    servlet:
        # 应用的访问路径
        context-path: /api
mybatis-plus:
    configuration:
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 查看日志
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/bianctf?characterEncoding=utf-8&useSSL=false
        username: root
        password: 123456
